<script>
  var displayGraph;
  $(document).ready(function () {


    console.log('document ready');
    displayGraph = function (data) {
      console.log( data );
      Chart.defaults.global.responsive = true;
      Chart.defaults.global.tooltipFillColor = 'rgb(44, 62, 80)';

      var barChartData = {
        labels: data.labels,
        datasets: [
          {
            fillColor : "rgba(122, 151, 190, 1)",
            strokeColor : "rgba(122, 151, 190, 1)",
            data: data.counts
          }
        ]
      };
      var ctx = $("#myChart").get(0).getContext("2d");

      var myChart = new Chart(ctx).Bar(barChartData, {responsive: true});
    }
    requestInfo();
  });
</script>

<div class="main__heading">
  <h2>Stats <i class="fa fa-bar-chart"></i></h2>
</div>

<div class="main__subheading">
</div>

<div class="graph-container">
  <h4 class="graph-title">My bookmark counts</h4>
  <!-- <svg id="graph"></svg> -->
  <canvas id="myChart"></canvas>
  <p class="text-center">Total number of bookmarks: <%= @bookmark_total %></p>
</div>
